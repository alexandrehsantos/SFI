//Modulo Principal 
var appListas = angular.module("appListas", [ 'ngroute' ]);

appCliente.config(function($routeProvider) {
	$routeProvider
		.when("/listas", {templateUrl : '/view/lista.html', controller : 'lista-controller'})
	    .when("/estado", {})
		});
